import { contract, then, when } from '../../utils/bdd';

contract('OTCPool', () => {
  describe('claim profits', () => {
    when('trade has been executed on otc pool', () => {
      then('otc provider gets tokens');
    });
  });
});
